# DockerMaid Environment Variables

# Server port (default: 3000)
PORT=3000

# API authentication token (optional)
# If set, all API requests must include Authorization: Bearer <token>
API_TOKEN=

# Development only - Frontend API URL (Vite dev server uses proxy)
# VITE_API_URL=/api
# VITE_API_TOKEN=your-token
